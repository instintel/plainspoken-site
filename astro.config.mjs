import { defineConfig } from 'astro/config';

// Private MVP phase: no site URL yet. Set `site` once a domain is chosen —
// Astro uses it for canonical URLs, RSS, and sitemap generation.
export default defineConfig({});
